#include "FileCacheManager.h"

server_side::FileCacheManager::FileCacheManager(string fileName)
{

}

server_side::FileCacheManager::~FileCacheManager()
{
    ///// close the file
}

bool server_side::FileCacheManager::isSolutionCashed(string*)
{
    ////////////
    return false;
}

string* server_side::FileCacheManager::getSolution(string*)
{
    ////////////
}

void server_side::FileCacheManager::cache(string* p, string* s)
{
    ////////////
}
